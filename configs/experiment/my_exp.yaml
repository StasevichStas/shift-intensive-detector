# @package _global_

defaults:
  - override /data: cs_datamodule
  - override /model: base_det
  - override /callbacks: default
  - override /trainer: gpu


tags: ["my_exp", "rcnn", "convnext_base"]
experiment_name: 'baseline_convnext_base'
seed: 12345

#ckpt_path: для возобновления эксперимента

trainer:
  min_epochs: 10
  max_epochs: 10
  gradient_clip_val: 0.5

model:

  optimizer:
    lr: 1e-3

  scheduler:
    _target_: "src.lit_modules.schedulers.ChainCyclicLR"
    _partial_: true
    lr_min: 5e-5
    lr_max: 1e-3
    T0: 300
    warmup_steps: 100
    scale_factor: 0.7

data:
  batch_size: 4
